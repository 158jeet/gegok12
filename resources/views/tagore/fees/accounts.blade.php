<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tagore Accounts</title><style>body{font-family:system-ui,sans-serif;background:#f6f7f9;margin:0;color:#17202a}.wrap{max-width:1400px;margin:30px auto;padding:0 18px}.card{background:#fff;border:1px solid #e5e7eb;border-radius:14px;padding:20px;margin-bottom:18px}.grid{display:grid;grid-template-columns:repeat(6,1fr);gap:10px}.metric{padding:14px;background:#f8fafc;border-radius:10px}.metric b{display:block;font-size:20px;margin-top:5px}.table{width:100%;border-collapse:collapse;min-width:1100px}.table th,.table td{text-align:left;padding:10px;border-bottom:1px solid #eee;vertical-align:top}.due{font-weight:700}.btn{display:inline-block;padding:7px 10px;border:1px solid #d1d5db;border-radius:8px;text-decoration:none;color:#17202a;background:#fff;cursor:pointer}.pill{display:inline-block;padding:3px 8px;border-radius:999px;background:#eef2ff;font-size:12px}.matched{background:#dcfce7}.mismatch{background:#fee2e2}.pending{background:#fef3c7}.form{display:grid;grid-template-columns:repeat(5,minmax(120px,1fr));gap:8px}.form input{padding:8px;border:1px solid #d1d5db;border-radius:8px}@media(max-width:900px){.grid{grid-template-columns:repeat(2,1fr)}.form{grid-template-columns:1fr}.card{overflow:auto}}</style></head><body><div class="wrap">
<div class="card"><h1>Tagore Accounts</h1><div>Fee collection, outstanding balances and online settlement reconciliation for your permitted institutions.</div><p><a class="btn" href="{{ route('tagore.fees.import') }}">Import Legacy Fee Data</a> <a class="btn" href="{{ route('tagore.fees.manage') }}">Manage Fee Structures</a></p></div>
<div class="grid"><div class="metric">Gross<b>₹{{ number_format($summary->gross ?? 0,2) }}</b></div><div class="metric">Discount<b>₹{{ number_format($summary->discount ?? 0,2) }}</b></div><div class="metric">Concession<b>₹{{ number_format($summary->concession ?? 0,2) }}</b></div><div class="metric">Payable<b>₹{{ number_format($summary->payable ?? 0,2) }}</b></div><div class="metric">Collected<b>₹{{ number_format($summary->paid ?? 0,2) }}</b></div><div class="metric">Outstanding<b class="due">₹{{ number_format($summary->outstanding ?? 0,2) }}</b></div></div>
<div class="card"><h2>Institutions</h2><table class="table"><tr><th>Institution</th></tr>@forelse($institutions as $institution)<tr><td>{{ $institution->display_name }}</td></tr>@empty<tr><td>No permitted institutions.</td></tr>@endforelse</table></div>
<div class="card"><h2>Online reconciliation</h2><p><span class="pill pending">{{ $pendingReconciliation }} pending</span> Successful online payments should be matched against the gateway settlement report. A mismatch is saved for review and does not alter the fee ledger.</p></div>
<div class="card"><h2>Payment transactions</h2><table class="table"><tr><th>Receipt</th><th>Student</th><th>Institution</th><th>Date</th><th>Mode / Gateway</th><th>Gateway IDs</th><th>Reference</th><th>Reconciliation</th><th>Amount</th><th>Actions</th></tr>@forelse($recent as $payment)<tr><td>{{ $payment->receipt_no ?: '—' }}</td><td>{{ $payment->student }}</td><td>{{ $payment->institution }}</td><td>{{ $payment->paid_at ? \Carbon\Carbon::parse($payment->paid_at)->format('d M Y H:i') : '—' }}</td><td>{{ ucfirst(str_replace('_',' ',$payment->payment_mode ?: ($payment->gateway ?: '—'))) }}</td><td>{{ $payment->gateway_order_id ?: '—' }}<br>{{ $payment->gateway_payment_id ?: '—' }}</td><td>{{ $payment->reference_number ?: '—' }}</td><td><span class="pill {{ $payment->reconciliation_status ?: 'pending' }}">{{ ucfirst($payment->reconciliation_status ?: 'pending') }}</span>@if($payment->settlement_id)<br>{{ $payment->settlement_id }} / ₹{{ number_format($payment->settlement_amount,2) }}@endif</td><td>₹{{ number_format($payment->amount,2) }}</td><td><a class="btn" href="{{ route('tagore.fees.receipt',$payment->id) }}">Receipt</a>@if(in_array($payment->gateway,['razorpay','online'],true))<details style="margin-top:6px"><summary>Reconcile</summary><form class="form" method="POST" action="{{ route('tagore.fees.reconcile',$payment->id) }}" style="margin-top:8px">@csrf<input name="settlement_id" placeholder="Settlement ID" value="{{ $payment->settlement_id }}"><input name="settlement_amount" type="number" min="0" step="0.01" placeholder="Settlement amount" value="{{ $payment->settlement_amount ?? $payment->amount }}" required><input name="settlement_date" type="date" value="{{ $payment->settlement_date ?? now()->toDateString() }}" required><input name="notes" placeholder="Notes"><button class="btn" type="submit">Save</button></form></details>@endif</td></tr>@empty<tr><td colspan="10">No successful payments recorded.</td></tr>@endforelse</table></div>
</div></body></html>
