<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $department->name }} | TagoreK12</title><style>body{font-family:system-ui,-apple-system,sans-serif;background:#f4f7fb;color:#172033;margin:0}.shell{max-width:1200px;margin:auto;padding:24px 16px 60px}.card{background:#fff;border:1px solid #e2e8f0;border-radius:12px;padding:16px;margin-top:16px}.grid{display:grid;grid-template-columns:repeat(6,1fr);gap:12px}.metric b{display:block;font-size:26px}.muted{color:#64748b;font-size:13px}table{width:100%;border-collapse:collapse}th,td{padding:9px;border-bottom:1px solid #edf2f7;text-align:left;font-size:13px}a{color:#2563eb;text-decoration:none}.tag{background:#eef2ff;padding:3px 7px;border-radius:999px;font-size:12px}@media(max-width:900px){.grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:550px){.grid{grid-template-columns:repeat(2,1fr)}}</style></head><body><div class="shell"><p><a href="{{ route('tagore.dashboard') }}">← Manager Command Center</a></p><div class="card"><h1 style="margin:0">{{ $department->name }}</h1><div class="muted">{{ $department->code }} · {{ $department->institution }}</div></div>
<div class="grid">@foreach([['Assigned',$stats['assigned']],['Active',$stats['active']],['Completed',$stats['completed']],['Overdue',$stats['overdue']],['Blocked',$stats['blocked']],['Completion',$stats['completion_rate'].'%']] as $m)<div class="card metric"><span class="muted">{{ $m[0] }}</span><b>{{ $m[1] }}</b></div>@endforeach</div>
<div class="card"><h2>7-day workload trend</h2><table><tr><th>Date</th><th>Created</th><th>Completed</th><th>Net</th></tr>@foreach($trend as $day)<tr><td>{{ date('d M',strtotime($day->day)) }}</td><td>{{ $day->created }}</td><td>{{ $day->completed }}</td><td>{{ $day->created-$day->completed }}</td></tr>@endforeach</table></div>
<div class="card"><h2>Employee workload</h2><table><tr><th>Employee</th><th>Designation</th><th>Active</th><th>Completed</th><th>Overdue</th><th>Blocked</th><th>Completion</th></tr>@forelse($employees as $employee)<tr><td><a href="{{ route('tagore.tasks.employee',$employee->id) }}">{{ $employee->name }}</a></td><td>{{ $employee->designation ?: '—' }}</td><td>{{ $employee->active }}</td><td>{{ $employee->completed }}</td><td>{{ $employee->overdue }}</td><td>{{ $employee->blocked }}</td><td>{{ $employee->completion_rate }}%</td></tr>@empty<tr><td colspan="7">No department staff mapped.</td></tr>@endforelse</table></div>
<div class="card"><h2>Pending follow-ups</h2>@if($followups->isEmpty())<div class="muted">No pending action-required follow-ups.</div>@else<table><tr><th>Employee</th><th>Task</th><th>Outcome</th><th>Follow-up</th></tr>@foreach($followups as $f)<tr><td>{{ $f->employee_name }}</td><td>{{ $f->task_title ?: '—' }}</td><td>{{ ucfirst(str_replace('_',' ',$f->outcome)) }}</td><td>{{ $f->follow_up_at ? date('d M Y H:i',strtotime($f->follow_up_at)) : 'Unscheduled' }}</td></tr>@endforeach</table>@endif</div></div></body></html>